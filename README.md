# Pro Converter

Image minifier, converter, and background remover.

## Run with Docker

```bash
docker compose up --build
```

Open http://localhost:3000

## Dev

```bash
npm install
pip install "rembg[cpu,cli]"
npm run dev
```
